<!DOCTYPE html>
<html lang="es" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Buscar Anime - AnimeHub</title>
  <link rel="stylesheet" href="/styles-tailwind.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Satoshi:wght@400;500;700&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'">
</head>
<body class="bg-body text-text font-satoshi min-h-screen flex flex-col">
  <header class="bg-body sticky top-0 z-40 border-b border-line/20">
    <div class="container relative flex items-center justify-between gap-6 py-4 lg:py-6">
      <div class="flex flex-1 gap-6 xl:flex-none">
        <figure>
          <a href="/" class="flex items-center gap-3">
            <i class="fas fa-play-circle text-2xl text-main"></i>
            <span class="text-lead font-bold text-xl">Anime<span class="text-main">Hub</span></span>
          </a>
        </figure>
        
        <div class="hidden xl:block xl:w-72">
          <form class="flex relative" method="get" action="/search" autocomplete="off">
            <div class="absolute left-3 top-1/2 transform -translate-y-1/2 text-subs">
              <i class="fas fa-search"></i>
            </div>
            <input 
              class="min-h-10 w-full bg-soft border border-line/40 rounded-lg pl-10 pr-20 text-sm text-text placeholder-subs focus:border-main focus:outline-none" 
              type="search" 
              name="q" 
              value="<%= query %>"
              placeholder="Buscar anime..."
            >
            <a class="absolute right-2 top-1/2 transform -translate-y-1/2 px-2 py-1 bg-line text-subs rounded text-xs hover:bg-edge transition-colors" href="/catalogo">
              <i class="fas fa-sliders-h mr-1"></i>Filtros
            </a>
          </form>
        </div>
      </div>
      
      <nav class="hidden lg:flex">
        <ul class="flex gap-1">
          <li>
            <a class="flex items-center gap-2 px-3 py-2 text-sm text-subs hover:text-lead hover:bg-soft rounded-lg transition-colors" href="/">
              <i class="fas fa-home"></i>
              <span>Inicio</span>
            </a>
          </li>
          <li>
            <a class="flex items-center gap-2 px-3 py-2 text-sm text-main bg-soft rounded-lg" href="/search">
              <i class="fas fa-search"></i>
              <span>Buscar</span>
            </a>
          </li>
          <li>
            <a class="flex items-center gap-2 px-3 py-2 text-sm text-subs hover:text-lead hover:bg-soft rounded-lg transition-colors" href="/catalogo">
              <i class="fas fa-th-large"></i>
              <span>Catálogo</span>
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="py-6 flex-1">
    <div class="container">
      <!-- Search Hero Section -->
      <section class="text-center py-12 mb-8">
        <div class="max-w-2xl mx-auto">
          <h1 class="text-4xl font-bold text-lead mb-4 flex items-center justify-center gap-3">
            <i class="fas fa-search text-main"></i>
            <span>Buscar Anime</span>
          </h1>
          <p class="text-lg text-subs mb-8">Encuentra tu anime favorito entre miles de títulos</p>
          
          <div class="max-w-lg mx-auto">
            <form class="flex relative" method="get" action="/search" autocomplete="off">
              <div class="absolute left-4 top-1/2 transform -translate-y-1/2 text-subs">
                <i class="fas fa-search"></i>
              </div>
              <input 
                class="w-full bg-soft border border-line rounded-lg pl-12 pr-20 py-4 text-text placeholder-subs focus:border-main focus:outline-none" 
                type="search" 
                name="q" 
                value="<%= query %>"
                placeholder="Escribe el nombre del anime..."
                autocomplete="off"
              >
              <button type="submit" class="absolute right-2 top-1/2 transform -translate-y-1/2 px-4 py-2 bg-main text-fore rounded-lg hover:opacity-90 transition-opacity">
                <i class="fas fa-arrow-right"></i>
              </button>
            </form>
          </div>
        </div>
      </section>

      <!-- Search Results Section -->
      <% if (hasSearched) { %>
        <section>
          <div class="mb-6">
            <% if (searchResults.length > 0) { %>
              <h2 class="text-2xl font-bold text-lead mb-2 flex items-center gap-3">
                <i class="fas fa-check-circle text-wins"></i>
                <span>Resultados para "<%= query %>"</span>
              </h2>
              <p class="text-sm text-subs"><%= searchResults.length %> animes encontrados</p>
            <% } else { %>
              <h2 class="text-2xl font-bold text-lead mb-2 flex items-center gap-3">
                <i class="fas fa-exclamation-circle text-warn"></i>
                <span>Sin resultados para "<%= query %>"</span>
              </h2>
            <% } %>
          </div>

          <% if (searchResults.length === 0) { %>
            <div class="text-center py-16 bg-soft border border-line rounded-lg">
              <div class="text-4xl mb-4 text-line">
                <i class="fas fa-search"></i>
              </div>
              <h3 class="text-xl text-lead mb-2">No se encontraron resultados</h3>
              <p class="text-subs mb-6">Intenta con otros términos de búsqueda</p>
              <div class="text-left max-w-md mx-auto">
                <p class="text-sm font-semibold text-lead mb-2">Sugerencias:</p>
                <ul class="text-sm text-subs space-y-1">
                  <li>• Usa términos más generales</li>
                  <li>• Verifica la ortografía</li>
                  <li>• Intenta con el nombre en japonés o inglés</li>
                  <li>• Busca por género o año</li>
                </ul>
              </div>
            </div>
          <% } else { %>
            <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 xl:grid-cols-5 xl:gap-6">
              <% searchResults.forEach((anime, index) => { %>
                <article class="group/item relative text-body">
                  <div class="relative bg-current">
                    <figure class="rounded-lg bg-black">
                      <img class="aspect-poster w-full rounded-lg object-cover" src="<%= anime.poster %>" alt="Portada de <%= anime.title %>" loading="lazy"/>
                    </figure>
                    <div class="absolute bottom-0 start-0 z-10 flex gap-2 rounded-se-lg bg-current pe-2 pt-2">
                      <div class="rounded bg-line px-2 py-1 text-xs font-bold text-subs">TV Anime</div>
                    </div>
                  </div>
                  <header class="grid items-start gap-1 p-2">
                    <h3 class="line-clamp-2 text-sm font-bold text-lead"><%= anime.title %></h3>
                  </header>
                  <a class="absolute inset-0 z-20 grid place-content-center" href="/media/<%= anime.slug %>">
                    <span class="sr-only">Ver <%= anime.title %></span>
                  </a>
                </article>
              <% }); %>
            </div>
          <% } %>
        </section>
      <% } else { %>
        <!-- Popular Searches Section -->
        <section class="text-center">
          <div class="mb-8">
            <h2 class="text-2xl font-bold text-lead mb-2 flex items-center justify-center gap-3">
              <i class="fas fa-fire text-mark"></i>
              <span>Búsquedas Populares</span>
            </h2>
          </div>
          <div class="flex flex-wrap justify-center gap-3 max-w-2xl mx-auto">
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="naruto">Naruto</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="one piece">One Piece</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="attack on titan">Attack on Titan</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="demon slayer">Demon Slayer</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="dragon ball">Dragon Ball</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="my hero academia">My Hero Academia</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="jujutsu kaisen">Jujutsu Kaisen</button>
            <button class="px-4 py-2 bg-soft border border-line text-subs rounded-full hover:bg-main hover:text-fore hover:border-main transition-all cursor-pointer" data-search="death note">Death Note</button>
          </div>
        </section>
      <% } %>
    </div>
  </main>

  <footer class="mt-auto">
    <div class="container flex flex-col flex-wrap items-center justify-center gap-4 py-12 md:flex-row lg:justify-between">
      <div class="flex flex-col items-center gap-x-6 gap-y-4 py-2 md:flex-row">
        <figure>
          <a href="/" class="flex items-center gap-2">
            <i class="fas fa-play-circle text-main text-xl"></i>
            <span class="font-bold text-lead">Anime<span class="text-main">Hub</span></span>
          </a>
        </figure>
        <span class="inline-flex items-center gap-3 text-sm font-medium">
          <i class="fas fa-heart text-tone text-xl"></i> By fans for fans
        </span>
      </div>
    </div>
  </footer>
  
  <!-- Mobile Navigation -->
  <nav class="bg-body sticky bottom-0 z-30 lg:hidden border-t border-line/20">
    <div class="container flex px-0">
      <a class="flex flex-1 flex-col items-center justify-center h-16 px-0 text-subs hover:text-main" href="/">
        <i class="fas fa-home text-2xl mb-1"></i>
        <span class="text-xs">Inicio</span>
      </a>
      <a class="flex flex-1 flex-col items-center justify-center h-16 px-0 text-main" href="/search">
        <i class="fas fa-search text-2xl mb-1"></i>
        <span class="text-xs">Buscar</span>
      </a>
      <a class="flex flex-1 flex-col items-center justify-center h-16 px-0 text-subs hover:text-main" href="/catalogo">
        <i class="fas fa-th-large text-2xl mb-1"></i>
        <span class="text-xs">Catálogo</span>
      </a>
      <a class="flex flex-1 flex-col items-center justify-center h-16 px-0 text-subs hover:text-main" href="/horario">
        <i class="fas fa-calendar text-2xl mb-1"></i>
        <span class="text-xs">Horario</span>
      </a>
    </div>
  </nav>
  
  <script>
    // Popular search tags
    document.querySelectorAll('[data-search]').forEach(tag => {
      tag.addEventListener('click', function() {
        const searchTerm = this.dataset.search;
        window.location.href = `/search?q=${encodeURIComponent(searchTerm)}`;
      });
    });
    
    // Image retry logic
    document.addEventListener('DOMContentLoaded', function() {
      document.querySelectorAll('img').forEach(img => {
        let retries = 0;
        const maxRetries = 3;
        
        img.addEventListener('error', function() {
          if (retries < maxRetries) {
            retries++;
            setTimeout(() => {
              this.src = this.src + (this.src.includes('?') ? '&' : '?') + 't=' + Date.now();
            }, 1000 * retries);
          }
        });
      });
    });
  </script>
</body>
</html>